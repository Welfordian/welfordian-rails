<!DOCTYPE html>
<html>
<head>
  <title>Blog</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss/dist/tailwind.min.css" rel="stylesheet">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => 'reload' %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => 'reload' %>
</head>

<body>
<nav class="flex items-center justify-between flex-wrap bg-blue-darker p-3 mb-6 shadow-lg fixed w-full pin-t z-10">
  <div class="flex items-center flex-no-shrink text-white mr-6">
    <span class="font-semibold text-xl tracking-tight">Joshua Welford</span></div>
  <div class="block lg:hidden">
    <button class="flex items-center px-3 py-2 border rounded text-blue-lighter border-blue-light hover:text-white hover:border-white">
      <svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg" class="fill-current h-3 w-3">
        <title>Menu</title>
        <path d="M0 3h20v2H0V3zm0 6h20v2H0V9zm0 6h20v2H0v-2z"></path>
      </svg>
    </button>
  </div>
  <div class="w-full block flex-grow lg:flex lg:items-center lg:w-auto hidden">
    <div class="text-sm lg:flex-grow">
      <a href="/" class="nav-link block mt-4 lg:inline-block lg:mt-0 hover:text-white mr-4
        <% if (request.env['PATH_INFO'] === "/") %> jw-link-active text-white no-underline
        <% else %> text-blue-lighter
        <% end %>">
        Blog
      </a>
      <a href="/contact" class="nav-link block mt-4 lg:inline-block lg:mt-0 hover:text-white
        <% if (request.env['PATH_INFO'] === "/contact") %> text-white no-underline
        <% else %> text-blue-lighter
        <% end %>">
        Contact
      </a> <!--<a href="/dashboard" class="ml-4 nav-link block mt-4 lg:inline-block lg:mt-0 hover:text-white text-blue-lighter">Dashboard</a>-->
    </div>
    <div>
      <a href="/login" class="nav-btn inline-block text-sm px-4 py-2 leading-none border rounded text-white border-white no-underline hover:border-transparent hover:text-grey-darker hover:bg-white mt-4 lg:mt-0">Login</a>
    </div>
  </div>
</nav>
<%= yield %>
</body>
</html>
